server:
  port: ${MOVIE_MATE_USER_SERVICE_PORT:7030}
  servlet:
    context-path: ${USER_SERVICE_CONTEXT_PATH:/api/v1}

spring:
  datasource:
    url: ${USER_SERVICE_DATASOURCE_URL}
    username: ${USER_SERVICE_DATASOURCE_USERNAME}
    password: ${USER_SERVICE_DATASOURCE_PASSWORD}
  jpa:
    properties:
      hibernate:
        show_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  cache:
    type: redis
    host: ${USER_SERVICE_REDIS_HOST}
    port: ${USER_SERVICE_REDIS_PORT}
    password: ${USER_SERVICE_REDIS_PASSWORD}
    ttl: ${USER_SERVICE_REDIS_TTL}
  application:
    name: user-service

security:
  jwt:
    secret-key: ${JWT_SECRET_KEY}
    access-expiration: ${JWT_ACCESS_EXPIRATION_TIME}
    refresh-expiration: ${JWT_REFRESH_EXPIRATION_TIME}
  root-user:
    username: ${USER_SERVICE_ROOT_USERNAME}
    email: ${USER_SERVICE_ROOT_EMAIL}
    password: ${USER_SERVICE_ROOT_PASSWORD}

logging:
  level:
    org:
      springframework: INFO

springdoc:
  api-docs:
    path: /docs
  swagger-ui:
    path: /docs/swagger-ui.html